# claude-switch 사용설명서

Claude Desktop에서 **여러 계정을 번갈아 쓰는** 도구입니다.
계정을 바꿔도 **로그인이 유지**되고, 수십 기가바이트짜리 파일을 다시 내려받지 않습니다.

컴퓨터를 잘 몰라도 괜찮습니다. 아래 순서만 따라 하면 됩니다.

> 이 도구는 Anthropic의 공식 프로그램이 아닙니다. 개인이 만든 도구입니다.

---

## 1. 준비물 확인

- **Windows 10 또는 11**
- **Microsoft Store에서 설치한 Claude Desktop**
  - 홈페이지에서 내려받아 설치한 버전은 **동작하지 않습니다.**
  - 확실하지 않으면 그냥 3번으로 넘어가세요. 프로그램이 알아서 확인해 줍니다.
- Claude Desktop에 **한 번은 로그인**해 둔 상태

---

## 2. 압축 풀기

1. 내려받은 `claude-switch-<버전>.zip` 파일을 **마우스 오른쪽 버튼**으로 클릭합니다.
2. **압축 풀기**(또는 "모두 압축 풀기")를 누릅니다.
3. 원하는 위치를 고르고 **압축 풀기**를 누릅니다.
   - 바탕화면이나 `문서` 폴더처럼 나중에 찾기 쉬운 곳을 권합니다.

> **중요:** 압축을 풀지 않고 zip 파일 안에서 바로 실행하면 동작하지 않습니다.
> 반드시 압축을 풀어서 **폴더로 꺼낸 뒤** 실행하세요.
>
> 그리고 폴더 안의 파일들은 **항상 같이 두세요.** 하나만 따로 옮기면 동작하지 않습니다.

---

## 3. 실행

압축을 푼 폴더에서 **`시작하기.cmd`** 를 두 번 클릭하세요.

### "Windows가 PC를 보호했습니다" 창이 뜨면

인터넷에서 내려받은 파일이라 Windows가 한 번 확인하는 것입니다. 정상입니다.

1. **추가 정보**를 클릭
2. **실행**을 클릭

### 검은 창이 열리고 목록이 나옵니다

```
=== claude-switch ===
Active: main

  1) main  [active]
  N) Add new profile
  Q) Quit

Select:
```

- `Active:` 뒤에 있는 것이 **지금 쓰고 있는 계정**입니다.
- 처음 실행하면 지금 로그인된 계정이 자동으로 **`main`** 이라는 이름을 받습니다.
- 번호를 입력하고 **Enter**를 누르면 그 계정으로 바뀌고 Claude가 열립니다.
- 그냥 닫고 싶으면 `Q`를 누르고 Enter.

---

## 4. 두 번째 계정 추가하기

1. `시작하기.cmd` 실행
2. **`N`** 을 누르고 Enter
3. 새 계정의 **이름**을 정해서 입력합니다
   - 영문, 숫자, `.`, `-`, `_` 만 쓸 수 있습니다
   - **한글과 공백은 안 됩니다**
   - 예: `work`, `personal`, `study`
4. Enter를 누르면 Claude가 새로 열립니다
5. 열린 Claude에서 **두 번째 계정으로 로그인**하세요

이제 끝났습니다. 다음부터는 `시작하기.cmd` 에서 번호만 고르면 계정이 바뀝니다.

---

## 5. 계정 바꾸기 (평소 사용법)

`시작하기.cmd` → 번호 입력 → Enter. 이게 전부입니다.

자주 쓰는 계정이 정해졌다면 더 빠른 방법도 있습니다.

| 파일 | 하는 일 |
| --- | --- |
| `시작하기.cmd` | 목록을 보고 번호로 고르기 (가장 쉬움) |
| `1-main.cmd` | `main` 계정으로 바로 전환 |
| `2-work.cmd` | `work` 계정으로 바로 전환 |
| `list.cmd` | 계정 목록만 확인 |
| `stop.cmd` | Claude를 완전히 종료 (아래 6번 참고) |

`2-work.cmd` 는 계정 이름이 `work` 일 때만 동작합니다. 다른 이름을 쓴다면 `시작하기.cmd`
를 쓰거나, 이 파일을 복사해서 안의 `work` 부분을 본인 계정 이름으로 바꿔주세요.

---

## 6. Claude Desktop을 업데이트할 때 (꼭 읽어주세요)

**업데이트 전에 `stop.cmd` 를 먼저 실행하세요.**

Claude Desktop은 창의 X를 눌러 닫아도 **뒤에서 계속 돌아가는 부분**이 남습니다. 그 상태로
업데이트를 하면 이런 오류가 나면서 실패하고, 보통 **재부팅**을 해야 풀립니다.

```
다른 프로그램이 현재 이 파일을 사용 중입니다.
(Another program is currently using this file.)
```

그래서 순서는 이렇게 하세요.

1. `1-main.cmd` 로 **`main` 계정으로 전환**해 둡니다
2. `stop.cmd` 를 실행해 Claude를 **완전히 종료**합니다
3. 그 다음에 업데이트를 진행합니다

이미 업데이트가 위 오류로 막혀버렸어도 괜찮습니다. `stop.cmd` 를 실행하면 재부팅 없이
풀립니다.

---

## 7. 문제가 생겼을 때

### 계정이 안 바뀌고 오류가 납니다

Claude가 완전히 닫히지 않은 경우가 대부분입니다. `stop.cmd` 를 실행한 뒤 다시 시도하세요.

### "Claude Desktop을 찾을 수 없습니다" 라고 나옵니다

Microsoft Store 버전이 아닐 가능성이 높습니다. Microsoft Store에서 Claude를 설치하고,
한 번 로그인한 다음 다시 실행해 주세요.

### 창이 열렸다가 바로 사라집니다

압축을 풀지 않고 zip 파일 안에서 실행했을 때 이런 일이 생깁니다. 2번 단계를 다시 확인해
주세요.

### "another claude-switch operation is in progress" 라고 나옵니다

이전 작업이 비정상적으로 끝난 경우입니다. 5분 정도 기다린 뒤 다시 실행하면 자동으로
풀립니다.

### 그 외

`list.cmd` 를 실행해서 나오는 내용을 확인해 보세요. 지금 어떤 계정이 활성 상태인지 알 수
있습니다.

---

## 8. 자주 묻는 질문

**Q. 계정을 바꾸면 대화 기록이 사라지나요?**
아니요. 각 계정의 데이터는 따로 보관되고, 다시 그 계정으로 돌아가면 그대로 있습니다.

**Q. 계정을 바꿀 때마다 다시 로그인해야 하나요?**
아니요. 로그인 상태가 유지됩니다.

**Q. 두 계정을 동시에 쓸 수 있나요?**
아니요. 한 번에 하나만 쓸 수 있습니다. 이 도구는 그 하나를 빠르게 바꿔주는 역할입니다.

**Q. 용량을 많이 차지하나요?**
계정을 추가해도 무거운 공용 파일(약 11GB)은 **한 번만** 저장하고 함께 씁니다. 계정마다
따로 받지 않습니다.

**Q. 이 도구를 지우고 싶으면요?**
폴더를 삭제하면 됩니다. 다만 계정을 여러 개 만든 상태라면, 지우기 전에 `1-main.cmd` 로
`main` 계정으로 돌려놓는 것을 권합니다.

---

## 버전 확인

지금 쓰고 있는 버전을 확인하려면 폴더에서 PowerShell을 열고 다음을 입력하세요.

```powershell
.\claude-switch.ps1 -Version
```

---

더 자세한 기술적인 설명은 같은 폴더의 `README.md` 에 있습니다.

라이선스: [MIT](LICENSE)
